import React from 'react';
import { View, ScrollView } from 'react-native';


const GalleryList = ()=>{
  return (
    <ScrollView
      style={{
        backgroundColor: 'tomato',
        width: width
        }}
    >
      {this.renderImages()}

      </ScrollView>
  )
}
